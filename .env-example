# Telegram API credentials (get these from https://my.telegram.org)
API_HASH=Your Telegram API hash (a string provided by Telegram)
API_ID=Your Telegram API ID (a numerical value provided by Telegram)

# Bot credentials (obtain these from @BotFather on Telegram)
TOKEN=Your bot token (provided by @BotFather)
OWNER=Your Telegram user ID (as seen via @userinfobot)

# MongoDB configuration (if you're using MongoDB for storage)
MONGO_URI=Your MongoDB connection URI (e.g., mongodb+srv://username:password@cluster.mongodb.net/...)
MONGO_NAME=Name of the MongoDB collection (default is "Deepseek" if not set)

# Huggingface API configuration (for inference requests)
HUGGINGFACE=Your Huggingface API key (available from your Huggingface account)

# Force channel settings (for channel linking and configuration)
FORCE=Set to true to enable force sub channel features (true or false)
CHANNEL_LINK=Your force sub channel link URL (e.g., https://t.me/Private_Bots)
CHANNEL_USERNAME=Your force sub channel username (include the "@" prefix, e.g., @Private_Bots)

# Web server configuration
PORT=Port number on which to run your bot's web server (default is 8000)
